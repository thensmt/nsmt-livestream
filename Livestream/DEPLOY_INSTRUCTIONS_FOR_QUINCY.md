# NSMT Stats — DNS Setup
**For: Quincy**

I need you to add two DNS records to point `stats.thensmt.com` to Firebase Hosting.

Log in to wherever `thensmt.com` DNS is managed and add these records:

| Type | Name | Value |
|------|------|-------|
| A | stats | *(David will provide this IP after Firebase setup)* |
| A | stats | *(David will provide this IP after Firebase setup)* |

Firebase will give David the exact IP addresses once the hosting is configured. He'll send them to you. Just add both A records exactly as he sends them and let him know when it's done.

SSL/HTTPS is handled automatically by Firebase — nothing else needed on your end.
